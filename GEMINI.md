# Gemini / Antigravity Context

See `AGENTS.md` at the repo root for the full handoff brief (constraints,
reading order, execution mode, current status). It's kept in one file to
avoid drift between duplicate copies — this file exists only so tooling that
specifically looks for `GEMINI.md` still finds the instructions.
